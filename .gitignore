# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dữ liệu chạy thật của máy chủ — CSDL, video người dùng tải lên, bản dựng tạm.
# 2,1GB và là video riêng tư; git không phải chỗ của chúng.
server/data/

# Tư liệu tham khảo và bản xuất thử — 50MB video, không phải mã nguồn.
examples/

# Ảnh chụp màn hình rơi ở gốc dự án lúc bàn giao diện.
/*.png
/*.jpg
/*.jpeg

# Thư viện Python cài kèm cho mlx-whisper — dựng lại được bằng install script,
# không phải mã nguồn của dự án.
server/asr/pylibs/

# Sinh ra lúc cài trên máy chủ: môi trường Python của faster-whisper và mô hình
# nghe tải về (khoảng 1,5 GB). Cả hai đều dựng lại được bằng deploy/setup-ubuntu.sh.
server/asr/venv/
server/asr/hf-cache/

# Khoá API — KHÔNG BAO GIỜ commit
.env
.env.*
!.env.example
server/seg/pylibs/

# Video dựng thử trong plans — tái tạo được từ mã, mỗi tệp vài megabyte.
# Ảnh thì GIỮ: chúng là bằng chứng của quyết định thiết kế, và nhẹ.
plans/**/video/

# Remotion single-engine artifacts (media + render output)
out/
public/spike/
public/remotion/
public/remotion-preview/

# Vietnamese NLP python env (underthesea) — cài local mỗi máy, không commit
scripts/vi-segment/vienv/
scripts/vi-segment/install.log

# Tệp mẫu của trang đo màn Cắt — copy từ dự án thật, xem scripts/cut-preview/
scripts/cut-preview/mau.mp4
scripts/cut-preview/strip.jpg
scripts/cut-preview/envelope.json
scripts/cut-preview/daghep.mp4
